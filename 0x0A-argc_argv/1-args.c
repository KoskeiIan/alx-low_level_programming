#include "holberton.h"
#include <stdio.h>

/**
 * main - Entry ppoint
 * @argc: arguments
 * @argv: array of the arguments
 * Return: 0
 */
int main(int argc, char argv[])
{
	printf("%d\n", --argc);
	(void) argv;
	return (0);
}
