#!/bin/bash
#include <stdio.h>
/**
 * main -Prints string
 * description: prints "\"Programming is like building a multilingual puzzle"
 * return: 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzz;e");
	return (0);
}
