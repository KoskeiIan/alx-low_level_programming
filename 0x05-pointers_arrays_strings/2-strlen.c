#include "holberton.h"
/**
 *_strlen - return length of string
 *@s: string
 *Return: length of s
 *
 */
int _strlen(char *s)
{
  int i, length;

  for (i=0; s[i]; i++)
    {
      length++
	}
  return (length);
}
  
