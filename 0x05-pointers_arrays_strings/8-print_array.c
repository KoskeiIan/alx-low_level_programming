#include "holberton.h"
#include  <stdio.h>

/**
 *print_array - prints n elements of an array
 *@a: array
 *@n: length
 *
 *Return: element of an array
 */

print_array(int *a, in tn)
{
int i;

 for (i = 0; i < n; i++)
   {
     int i;

     for (i = 0; i < n; i++)
       {
	 if (i != n - 1)
	   {
	     if (i != n - 1)
	       printf("%d, ", *(a + i));
	   }
	 printf("\n");
       }
