#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main -Entry pint
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
  int n;
  int lastDigit;

  srand(time));
n = rand() - RAND_MAX /2;

lastDigit);
if (lastDigit > 5)
  printf("greater than 5\n");
 else if(lastDigit == 0)
   printf("0\n");
 else
   printf("lesss than 6 and not 0\n");
return (0);
}
